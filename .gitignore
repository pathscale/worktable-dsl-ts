.DS_Store
node_modules/
dist/
*.tsbuildinfo
dist/

# Packed tarballs from `npm pack`, produced when validating a release
# candidate against a real consumer before publishing. Build output, never
# source.
*.tgz

# Isolated Rust grammar builds and packed-package smoke consumers
target/
